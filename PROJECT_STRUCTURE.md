# 📁 專案結構說明

```
fintech-monitor/
│
├── 📄 README.md                    # 專案說明文檔
├── 📄 QUICKSTART.md                # 5分鐘快速入門
├── 📄 DEPLOYMENT_GUIDE.md          # 詳細部署指南
├── 📄 .gitignore                   # Git 忽略文件
│
├── 🚀 start.bat                    # Windows 一鍵啟動腳本
├── 🚀 start.sh                     # macOS/Linux 一鍵啟動腳本
│
├── 📂 backend/                     # 後端 API 服務
│   ├── main.py                     # FastAPI 主程式
│   ├── requirements.txt            # Python 依賴列表
│   └── .env (需自建)               # 環境變數配置
│
└── 📂 frontend/                    # 前端 React 應用
    ├── 📂 src/
    │   ├── App.jsx                 # 主要 React 組件
    │   ├── main.jsx                # React 入口文件
    │   └── index.css               # 全局樣式
    │
    ├── index.html                  # HTML 入口
    ├── package.json                # npm 依賴配置
    ├── vite.config.js              # Vite 構建配置
    ├── tailwind.config.js          # Tailwind CSS 配置
    └── postcss.config.js           # PostCSS 配置
```

---

## 📋 文件說明

### 根目錄文件

| 文件 | 用途 |
|------|------|
| `README.md` | 專案完整說明，包含功能介紹、技術棧、使用方法 |
| `QUICKSTART.md` | 快速入門指南，5分鐘上手教學 |
| `DEPLOYMENT_GUIDE.md` | 詳細的部署文檔，涵蓋各種部署場景 |
| `.gitignore` | Git 版本控制忽略規則 |
| `start.bat` | Windows 系統一鍵啟動腳本 |
| `start.sh` | macOS/Linux 系統一鍵啟動腳本 |

### 後端目錄 (`backend/`)

| 文件 | 說明 |
|------|------|
| `main.py` | FastAPI 應用主程式，包含所有 API 端點 |
| `requirements.txt` | Python 依賴包列表 |
| `.env` | 環境變數配置（需自行創建） |

### 前端目錄 (`frontend/`)

| 文件 | 說明 |
|------|------|
| `src/App.jsx` | 主要 React 組件，包含所有 UI 和業務邏輯 |
| `src/main.jsx` | React 應用入口點 |
| `src/index.css` | Tailwind CSS 和全局樣式 |
| `index.html` | HTML 模板文件 |
| `package.json` | npm 項目配置和依賴列表 |
| `vite.config.js` | Vite 構建工具配置 |
| `tailwind.config.js` | Tailwind CSS 自定義配置 |
| `postcss.config.js` | PostCSS 插件配置 |

---

## 🔧 運行後的目錄結構

首次運行後，會自動生成以下目錄：

```
backend/
└── venv/                   # Python 虛擬環境
    ├── Lib/               # (Windows)
    ├── lib/               # (macOS/Linux)
    └── Scripts/           # (Windows) 或 bin/ (macOS/Linux)

frontend/
├── node_modules/          # npm 依賴包
└── dist/                  # 生產構建輸出（運行 npm run build 後）
```

**注意**: 這些目錄已在 `.gitignore` 中，不會提交到版本控制。

---

## 📊 核心代碼分布

### 後端 API 端點 (`backend/main.py`)

- **`GET /`** - API 資訊
- **`GET /api/bond-spread`** - 美日利差數據
- **`GET /api/fx`** - USD/JPY 匯率數據
- **`GET /api/commodities`** - 黃金和原油數據
- **`GET /api/all`** - 一次性獲取所有數據

### 前端組件結構 (`frontend/src/App.jsx`)

```
App Component
├── Header
│   ├── 標題和圖標
│   ├── 週期選擇器
│   └── 刷新按鈕
│
├── Statistics Cards (4個卡片)
│   ├── 美日利差卡片
│   ├── USD/JPY 卡片
│   ├── 黃金卡片
│   └── 原油卡片
│
├── Charts Section
│   ├── 雙 Y 軸圖表 (利差 vs 匯率)
│   ├── 黃金價格走勢圖
│   └── 原油價格走勢圖
│
└── Footer
```

---

## 🎨 樣式架構

### Tailwind CSS 工具類

主要使用的 Tailwind 工具類：
- **佈局**: `flex`, `grid`, `container`
- **間距**: `px-4`, `py-6`, `space-x-3`
- **顏色**: `bg-white`, `text-gray-800`, `text-indigo-600`
- **響應式**: `md:`, `lg:` 前綴
- **效果**: `shadow-md`, `rounded-lg`, `hover:`

### 自定義樣式 (`index.css`)

- Tailwind 基礎樣式引入
- 自定義滾動條樣式
- 加載動畫（shimmer effect）

---

## 🔄 數據流向

```
用戶瀏覽器
    ↓
React App (localhost:3000)
    ↓ HTTP Request
FastAPI Server (localhost:8000)
    ↓ API Call
yfinance Library
    ↓ Web Scraping
Yahoo Finance
    ↓ Data Return
FastAPI → React → 用戶
```

---

## 📦 依賴關係

### 後端依賴

```
FastAPI (Web 框架)
    ├── Uvicorn (ASGI 伺服器)
    └── Pydantic (數據驗證)

yfinance (金融數據)
    └── pandas (數據處理)
```

### 前端依賴

```
React (UI 框架)
    ├── React DOM (DOM 操作)
    └── Vite (構建工具)

Recharts (圖表庫)
    └── D3.js (底層圖表引擎)

Tailwind CSS (樣式框架)
    ├── PostCSS (CSS 處理)
    └── Autoprefixer (瀏覽器兼容)

Lucide React (圖標庫)
```

---

## 🚀 首次啟動流程

1. **執行啟動腳本**
   - Windows: `start.bat`
   - macOS/Linux: `./start.sh`

2. **自動安裝依賴**
   - 後端: `pip install -r requirements.txt`
   - 前端: `npm install`

3. **啟動服務**
   - 後端: `uvicorn main:app --reload`
   - 前端: `npm run dev`

4. **打開瀏覽器**
   - 前端: http://localhost:3000
   - API 文檔: http://localhost:8000/docs

---

## 📝 開發建議

### 修改後端代碼

1. 修改 `backend/main.py`
2. Uvicorn 會自動重載（`--reload` 模式）
3. 在瀏覽器刷新測試

### 修改前端代碼

1. 修改 `frontend/src/App.jsx` 或其他文件
2. Vite 會自動熱重載（HMR）
3. 瀏覽器自動更新，無需手動刷新

### 添加新功能

1. **新的 API 端點**: 在 `main.py` 添加新的 `@app.get()` 函數
2. **新的圖表**: 在 `App.jsx` 添加新的 `<LineChart>` 組件
3. **新的數據源**: 在 `TICKERS` 字典添加新的 ticker 代號

---

## 🔍 故障排查指南

### 後端無法啟動

1. 檢查 Python 版本: `python --version`
2. 檢查虛擬環境是否啟動
3. 查看錯誤日誌: `uvicorn main:app`

### 前端無法啟動

1. 檢查 Node.js 版本: `node --version`
2. 刪除 `node_modules` 重新安裝: `rm -rf node_modules && npm install`
3. 檢查埠口 3000 是否被占用

### 數據無法加載

1. 檢查後端是否正常運行: http://localhost:8000/docs
2. 查看瀏覽器 Console 錯誤訊息
3. 檢查網絡連接（yfinance 需要訪問 Yahoo Finance）

---

**最後更新**: 2024-12-29  
**維護者**: James
